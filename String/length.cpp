#include <bits/stdc++.h>
using namespace std;
int main()
{
    int i;
    char s[] = "coder";
    for (i = 0; s[i] != '\0'; i++);
    cout << ("length of the string: %d", i);
    /* this is secound matherod to do this*/
    // char str[]="hello world";
    // cout<<strlen(str);
    return 0;
}